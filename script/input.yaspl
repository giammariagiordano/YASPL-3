head
string a, b;

def compare(in string a, in string b) {
	if(a == "ciao") then {
		string c = "sono uguali";
		c->;
	} else {
		string d = "sono diverse";
		d->;
	}

}

start
"inserisci la prima stringa"->;
a<-;
"inserisci la seconda stringa"->;
b<-;
compare(a,b);